import Engine from "./classes/core/Engine";
import SocketClient from "./classes/network/SocketClient";

let socket: SocketClient = new SocketClient();

window.addEventListener("DOMContentLoaded", () => {});
